name: CI
permissions:
  contents: read

on:
  push:
    branches: [main, master]
  pull_request:
  workflow_dispatch:

jobs:
  quality:
    name: Quality
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6
        with:
          fetch-depth: 1
          fetch-tags: false
          submodules: false

      - name: Install uv
        uses: astral-sh/setup-uv@5a095e7a2014a4212f075830d4f7277575a9d098 # v7
        with:
          version: "latest"
          enable-cache: true
          cache-dependency-glob: "uv.lock"

      - name: Set up Python
        run: uv python install 3.12

      - name: Install dependencies
        run: uv sync --all-extras --dev

      - name: Release hygiene checks
        run: |
          uv run python scripts/check_release_hygiene.py
          uv run python scripts/check_release_metadata.py

      - name: Docs quality checks
        run: uv run python scripts/check_docs_quality.py

      - name: Security checks
        run: |
          uvx pip-audit
          uvx bandit -q -r src/fleet_rlm -x tests,src/fleet_rlm/_scaffold -lll

      - name: Lint with Ruff
        run: |
          uv run ruff check src tests
          uv run ruff format --check src tests

      - name: Type-check with ty
        run: >
          uv run ty check src
          --exclude "src/fleet_rlm/_scaffold/**"
          --exclude "src/fleet_rlm/analytics/**"
          --exclude "src/fleet_rlm/daytona_rlm/**"

  test-unit:
    name: Test Unit
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6
      - name: Install uv
        uses: astral-sh/setup-uv@5a095e7a2014a4212f075830d4f7277575a9d098 # v7
        with:
          version: "latest"
          enable-cache: true
          cache-dependency-glob: "uv.lock"
      - name: Set up Python
        run: uv python install 3.12
      - name: Install dependencies
        run: uv sync --all-extras --dev
      - name: Run unit tests
        env:
          PYTEST_DISABLE_PLUGIN_AUTOLOAD: "1"
          MLFLOW_TRACKING_URI: "file:///tmp/mlruns"
        run: >
          uv run pytest -q tests/unit
          -p pytest_asyncio.plugin
          -p pytest_timeout
          --timeout=120
          --timeout-method=thread
          -m "not live_llm and not benchmark"

  test-ui:
    name: Test UI
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6
      - name: Install uv
        uses: astral-sh/setup-uv@5a095e7a2014a4212f075830d4f7277575a9d098 # v7
        with:
          version: "latest"
          enable-cache: true
          cache-dependency-glob: "uv.lock"
      - name: Set up Python
        run: uv python install 3.12
      - name: Install dependencies
        run: uv sync --all-extras --dev
      - name: Run UI tests
        run: uv run pytest -q tests/ui -m "not live_llm and not benchmark"

  test-integration:
    name: Test Integration
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6
      - name: Install uv
        uses: astral-sh/setup-uv@5a095e7a2014a4212f075830d4f7277575a9d098 # v7
        with:
          version: "latest"
          enable-cache: true
          cache-dependency-glob: "uv.lock"
      - name: Set up Python
        run: uv python install 3.12
      - name: Install dependencies
        run: uv sync --all-extras --dev
      - name: Run integration and e2e tests
        run: uv run pytest -q tests/integration tests/e2e -m "not live_llm and not benchmark"

  frontend-check:
    name: Frontend Check
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6

      - name: Detect frontend workspace
        id: frontend
        shell: bash
        run: |
          if [ -f src/frontend/package.json ]; then
            echo "enabled=true" >> "$GITHUB_OUTPUT"
          else
            echo "enabled=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Install pnpm
        if: steps.frontend.outputs.enabled == 'true'
        uses: pnpm/action-setup@v3
        with:
          version: 9.12.0

      - name: Set up Node.js
        if: steps.frontend.outputs.enabled == 'true'
        uses: actions/setup-node@v4
        with:
          node-version: '24'
          cache: 'pnpm'
          cache-dependency-path: 'src/frontend/pnpm-lock.yaml'

      - name: Install Vite+ CLI
        if: steps.frontend.outputs.enabled == 'true'
        run: npm install -g vite-plus@0.1.11

      - name: Frontend install
        if: steps.frontend.outputs.enabled == 'true'
        run: |
          cd src/frontend
          vp install

      - name: Install Playwright browser
        if: steps.frontend.outputs.enabled == 'true'
        run: |
          cd src/frontend
          pnpm dlx playwright install --with-deps chromium

      - name: Frontend quality gate
        if: steps.frontend.outputs.enabled == 'true'
        run: |
          cd src/frontend
          ./node_modules/.bin/vp check
          ./node_modules/.bin/vp test
          ./node_modules/.bin/vp build

      - name: Frontend check skipped
        if: steps.frontend.outputs.enabled != 'true'
        run: echo "No src/frontend/package.json found. Skipping frontend checks."
