<!-- Chunk 256: bytes 314940-315154, type=function -->
def get_active_user_emails(users):
    result = []
    for user in users:
        if user.is_active:
            if user.email:
                result.append(user.email)
    return result
```

**After:**
```python
