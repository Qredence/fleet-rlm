<!-- Chunk 1423: bytes 5512766-5513165, type=function -->
def add(x, y):
    return x + y

result = add(5, 3)
print(result)
```

This decorator logs the function name, arguments, and return value each time the decorated function is called.

# Caching with Decorators

Decorators can also be used to implement caching, which can significantly improve performance for functions that are called repeatedly with the same arguments.

```python
import functools

