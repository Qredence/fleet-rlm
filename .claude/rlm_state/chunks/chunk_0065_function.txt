<!-- Chunk 65: bytes 129853-130027, type=function -->
def divide_numbers(a: float, b: float) -> float:
    if b == 0:
        raise ValueError(f"Cannot divide by zero: {a} / {b}")
    return a / b

# Bad: Generic error messages
