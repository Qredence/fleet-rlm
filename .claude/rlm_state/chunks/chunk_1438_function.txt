<!-- Chunk 1438: bytes 5526156-5526896, type=function -->
def greet(name):
    """Greets the person passed as argument"""
    return f"Hello, {name}!"

print(greet.__name__)
print(greet.__doc__)
```

**Explanation:**

1.  We import the `functools` module.
2.  We use `@functools.wraps(func)` above the `wrapper` function definition. This decorator copies the metadata from the original function `func` to the `wrapper` function.

**Output:**

```
greet
Greets the person passed as argument
```

# Class Decorators

Class decorators are used to modify or enhance the behavior of a class. They can be used to add attributes, methods, or modify existing ones.

## Basic Class Decorator

A class decorator is simply a function that takes a class as an argument and returns a modified class.

```python
