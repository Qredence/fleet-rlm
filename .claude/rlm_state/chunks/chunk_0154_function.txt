<!-- Chunk 154: bytes 217739-217954, type=function -->
def quality_metric(example, pred, trace=None):
    score = 0.0
    if example.output == pred.output:
        score += 0.5
    if len(pred.output) > 50:
        score += 0.5
    return score

# Bad: Return only bool
