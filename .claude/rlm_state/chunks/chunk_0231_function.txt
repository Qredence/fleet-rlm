<!-- Chunk 231: bytes 307230-307532, type=function -->
def calculate_user_score(user: User) -> int:
    score = 0
    for activity in user.activities:
        if activity.type == "login":
            score += 1
        elif activity.type == "purchase":
            score += 5
        elif activity.type == "review":
            score += 3
    return score

