<!-- Chunk 710: bytes 1412576-1413819, type=function -->
def transactional_session():
    """Context manager for short-lived database transactions."""
    db = SessionLocal()
    try:
        yield db
        db.commit()
    except Exception:
        db.rollback()
        raise
    finally:
        db.close()
```

**Tasks**:
- [ ] Create `session.py` with `transactional_session()` context manager
- [ ] Add connection pooling configuration with proper timeouts
- [ ] Set `pool_pre_ping=True` to verify connections before use
- [ ] Configure `pool_recycle=300` (5 min) to recycle connections

#### Step 2: Refactor Workflow Operations (Days 2-3)

**Target files**:
- `src/skill_fleet/core/workflows/skill_creation/understanding.py`
- `src/skill_fleet/core/workflows/skill_creation/generation.py`
- `src/skill_fleet/core/workflows/skill_creation/validation.py`

**Tasks**:
- [ ] **Understanding phase**: Wrap DB reads/writes in `transactional_session()` blocks, extract LLM calls outside transactions
- [ ] **Generation phase**: Same pattern - fetch state → LLM call → save result
- [ ] **Validation phase**: Same pattern - fetch draft → validate → save report
- [ ] Add job state serialization/deserialization helpers for cross-transaction persistence

**Example refactoring**:
```python
# Before:
