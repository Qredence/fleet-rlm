<!-- Chunk 1428: bytes 5514427-5514807, type=function -->
def sensitive_data():
    return "This is sensitive data."

print(sensitive_data())
```

This example shows a basic authentication decorator.  The `authenticate_user` function would contain the actual authentication logic.

# Decorator Factories

Decorator factories are functions that return decorators. They allow you to create decorators with customizable behavior.

```python
