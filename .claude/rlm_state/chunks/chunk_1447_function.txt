<!-- Chunk 1447: bytes 5531065-5531477, type=function -->
def cache(func):
    """Caches the result of the function for given arguments."""
    @functools.wraps(func)
    def wrapper(*args, **kwargs):
        key = (args, tuple(sorted(kwargs.items())))  # Create a unique key for the arguments
        if key not in wrapper.cache:
            wrapper.cache[key] = func(*args, **kwargs)
        return wrapper.cache[key]
    wrapper.cache = {}
    return wrapper

@cache
