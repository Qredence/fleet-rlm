<!-- Chunk 1451: bytes 5533679-5534383, type=function -->
def retry(num_attempts, delay=1):
    """Retries a function a specified number of times with a delay."""
    def my_decorator(func):
        @functools.wraps(func)
        def wrapper(*args, **kwargs):
            for attempt in range(num_attempts):
                try:
                    return func(*args, **kwargs)
                except Exception as e:
                    print(f"Attempt {attempt + 1} failed: {e}")
                    if attempt == num_attempts - 1:
                        raise  # Re-raise the exception after the last attempt
                    time.sleep(delay)  # Wait before the next attempt
        return wrapper
    return my_decorator

@retry(num_attempts=3, delay=2)
