<!-- Chunk 1008: bytes 3552833-3554890, type=class -->
class ResultCache:
    def get(self, job_id: str) -> Optional[dict]:
        # 1. Check local file cache
        local_path = f"~/.skill_fleet/cache/jobs/{job_id}.json"
        if exists(local_path):
            # 2. Check remote ETag
            remote = api.head(f"/jobs/{job_id}")
            if remote["ETag"] == local["_etag"]:
                return load(local_path)  # ✅ Cache hit
        
        # 3. Fetch from API
        result = api.get(f"/jobs/{job_id}")
        self.save(job_id, result)
        return result
        
    def save(self, job_id: str, data: dict):
        # Store with ETag for future validation
        data["_etag"] = api.get_etag(job_id)
        write(f"~/.skill_fleet/cache/jobs/{job_id}.json", data)
```

**CLI Usage**:
```bash
uv run skill-fleet jobs results job_123
# 1st call: Fetches from API (~200ms)
# 2nd call within 5min: Returns from local cache (~10ms)
# Output: "Loaded from cache (123 days old, 8 bytes)"
```

**API Configuration** (in `api/config.py`):
```python
CACHE_TTL_SECONDS = 300  # Default 5 minutes
CACHE_REDIS_ENABLED = False  # Optional Redis for distributed
```

**Files to Create**:
1. `src/skill_fleet/cli/cache.py` (~150 lines)
2. Update `src/skill_fleet/api/config.py` with cache settings
3. Update `src/skill_fleet/api/app.py` with cache middleware
4. Update `src/skill_fleet/cli/client.py` to use cache

**Timeline**: 0.5 days (filesystem-only, no Redis) | **Impact**: 5-10x faster CLI result retrieval

---

### TIER 3: DSPy Workflow Refinement

#### **3A: Module Composition Registry** 🏭

**Problem**: Modules created ad-hoc in `skill_creator.py`; no factory pattern. Hard to swap, version, test.

**Solution**: Central registry for consistent module instantiation.

**Implementation**:
- New module: `src/skill_fleet/core/dspy/modules/registry.py`
  - Class: `ModuleRegistry`
  - Map: (phase, module_type, version) → module class
  - Factory: `registry.create(phase="phase1", type="understanding", config={...})`
  - Supports versioning, introspection

**Registry Structure**:
```python
