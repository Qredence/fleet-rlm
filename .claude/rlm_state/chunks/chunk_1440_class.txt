<!-- Chunk 1440: bytes 5527008-5527671, type=class -->
class MyClass:
    def __init__(self, name):
        self.name = name

instance = MyClass("Bob")
print(instance.name)
print(instance.extra_attribute)
```

**Explanation:**

1.  `my_class_decorator` is a decorator function that takes a class `cls` as an argument.
2.  Inside `my_class_decorator`, we add a new attribute `extra_attribute` to the class.
3.  `my_class_decorator` returns the modified class.
4.  The `@my_class_decorator` syntax applies the decorator to the `MyClass` class.

**Output:**

```
Bob
Added by decorator
```

## Decorators that Replace a Class

A class decorator can also replace the original class with a completely new class.

```python
