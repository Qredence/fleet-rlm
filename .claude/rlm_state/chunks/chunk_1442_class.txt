<!-- Chunk 1442: bytes 5528024-5529103, type=class -->
class OriginalClass:
    def __init__(self, name):
        self.name = name

    def display(self):
        print(f"Original class display: {self.name}")


instance = OriginalClass("Charlie")
instance.display()  # Calls the NewClass display method
print(instance.original.name) # Access the original class instance
```

**Explanation:**

1.  The `replace_class` decorator replaces the `OriginalClass` with `NewClass`.
2.  `NewClass` initializes the `OriginalClass` within itself, allowing you to still access the original functionality.
3.  The `@replace_class` syntax means `OriginalClass` now refers to `NewClass`.

**Output:**

```
New class instance created
Displaying via the NewClass wrapper
Charlie
```

# Parameterized Decorators

Parameterized decorators are decorators that accept arguments. This allows you to customize the behavior of the decorator based on the arguments you provide.

## Basic Parameterized Decorator

To create a parameterized decorator, you need to define a function that takes the decorator arguments and returns a decorator function.

```python
