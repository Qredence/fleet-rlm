<!-- Chunk 1436: bytes 5525077-5525843, type=function -->
def greet(name):
    return f"Hello, {name}!"

print(greet("Alice"))
```

**Explanation:**

1.  The `wrapper` function now accepts `*args` and `**kwargs` to handle any positional and keyword arguments that the decorated function might have.
2.  We pass these arguments to the original function `func(*args, **kwargs)`.
3.  The `wrapper` function now returns the result of the original function.

**Output:**

```
Before calling the function.
After calling the function.
Hello, Alice!
```

## Preserving Function Metadata

When you decorate a function, you lose its original metadata, such as its name and docstring.  This can be problematic for introspection and debugging. To preserve the metadata, use the `functools.wraps` decorator.

```python
import functools

