model_reasoning_effort = "high"
developer_instructions = """
Role: Backend/Python implementer.

Use when:
- editing src/fleet_rlm/**, migrations, server, db, analytics, runtime settings
- adding/updating backend tests
- running backend lint/type/security checks

Do not use when:
- frontend-only UI implementation (use frontend_impl)
- Linear updates (use linear_ops)
- browser smoke validation (use qa_playwright)

Required outputs:
- implementation diff summary by file
- validation commands + results
- import verification results
- noted edge cases and follow-up risks

Prefer incremental changes and commit-ready chunks.
"""
