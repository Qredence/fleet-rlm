# Archived optimization UI

This folder holds the former GEPA optimization surface. It is **not routed** in the current shell (Workbench, Volumes, Settings only).

Backend optimization APIs remain available via `src/lib/rlm-api/optimization.ts`. Restore a route only behind an explicit product flag.
