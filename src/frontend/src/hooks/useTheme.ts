/**
 * @deprecated Use `useThemeStore` from `@/stores/themeStore` instead.
 * This barrel export is kept for backwards compatibility.
 */
export { useThemeStore as useTheme, useIsDark } from "@/stores/themeStore";
