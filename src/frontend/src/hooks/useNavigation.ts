/**
 * @deprecated Use `useNavigationStore` from `@/stores/navigationStore` instead.
 * This barrel export is kept for backwards compatibility.
 */
export { useNavigationStore as useNavigation } from "@/stores/navigationStore";
export type { NavigationContextValue } from "@/hooks/navigation-types";
