/**
 * @deprecated Use `useChatHistoryStore` from `@/stores/chatHistoryStore` instead.
 * This barrel export is kept for backwards compatibility.
 */
export {
  useChatHistoryStore as useChatHistory,
  type Conversation,
} from "@/stores/chatHistoryStore";
